## 위상(topology)과 위상공간(topological spaces)
> [!todo]- 위상(topology)의 정의는 무엇인가?
> 
> 집합 $X$가 주어졌을 때, $X$의 부분집합들의 모임 $\mathcal{T}$가 다음 세 가지 조건을 만족하면, $\mathcal{T}$를 $X$위의 위상이라고 한다.
> 
> 1. 공집합 $\emptyset$과 전체집합 $X$는 $\mathcal{T}$의 원소이다.
>     
> 2. $\mathcal{T}$의 원소들의 임의의 합집합은 다시 $\mathcal{T}$의 원소이다.
>     
> 3. $\mathcal{T}$의 원소들의 유한 교집합은 다시 $\mathcal{T}$의 원소이다.
>     
> 
> 위상은 집합의 원소들 사이에 '가까움'이나 '연결성'과 같은 공간적 관계를 부여하는 구조이다. 이는 열린 집합(open set)의 개념을 일반화하여, 거리(metric) 개념 없이도 연속성, 수렴성 등의 개념을 정의할 수 있게 하는 최소한의 장치이다.

> [!todo]- 위상 공간(topological space)은 어떻게 정의되는가?
> 
> 집합 $X$와 그 위의 위상 $\mathcal{T}$의 순서쌍 $(X, \mathcal{T})$를 위상 공간이라고 한다. 이때 $\mathcal{T}$의 원소들을 X의 **열린 집합(open sets)**이라고 부른다. 어떤 집합이 위상 공간의 열린 집합의 여집합이면, 그 집합을 **닫힌 집합(closed set)**이라고 한다.
> 
> 위상 공간은 단순히 점들의 모임이 아니라, 열린 집합이라는 구조를 통해 '모양'을 갖게 된 집합이다. 이 구조 덕분에 우리는 유클리드 공간이 아닌 더 추상적인 공간에서도 함수의 연속성이나 공간의 연결성과 같은 기하학적 성질을 탐구할 수 있다.

> [!todo]- 더 세밀한(finer) 위상과 더 엉성한(coarser) 위상이란 무엇인가?
> 
> 집합 $X$ 위에 두 위상 $\mathcal{T}$와 $\mathcal{T'}$이 주어졌을 때, 만약 $\mathcal{T} \subset \mathcal{T'}$이면, 위상 $\mathcal{T'}$이 $\mathcal{T}$보다 더 세밀하다(finer) 또는 **더 강하다(stronger)** 고 말한다. 반대로 $\mathcal{T}$는 $\mathcal{T'}$보다 더 엉성하다(coarser) 또는 **더 약하다(weaker)** 고 한다.
> 
> 위상이 더 세밀하다는 것은 더 많은 열린 집합을 가지고 있다는 뜻이다. 이는 마치 공간을 더 잘게 쪼개어 자세히 들여다보는 것과 같다. 예를 들어, 어떤 집합 $X$에 대한 이산 위상(discrete topology)은 모든 부분집합을 열린 집합으로 가지므로 가장 세밀한 위상이며, 비이산 위상(indiscrete topology)은 $\emptyset$과 $X$만을 열린 집합으로 가지므로 가장 엉성한 위상이다.

## 위상의 기저(basis)

> [!todo]- 위상 공간의 기저(basis)란 무엇이며, 어떤 조건을 만족해야 하는가?
> 집합 $X$ 위의 위상 $\mathcal{T}$에 대한 **기저(basis)** $\mathcal{B}$는 다음 두 조건을 만족하는 $\mathcal{T}$의 부분 집합이다.
> 1. $X$의 모든 점 $x$에 대하여, $x \in B$를 만족하는 기저 원소 $B \in \mathcal{B}$가 적어도 하나 존재한다. 즉, $\bigcup_{B \in \mathcal{B}} B = X$이다.
> 2. $X$의 임의의 점 $x$가 두 기저 원소 $B_1, B_2$의 교집합에 포함된다면($x \in B_1 \cap B_2$), $x$를 포함하고 $B_1 \cap B_2$에 포함되는 또 다른 기저 원소 $B_3$가 존재한다($x \in B_3 \subset B_1 \cap B_2$).
> 
> 기저 $\mathcal{B}$가 주어지면, 이로부터 **생성된 위상(generated topology)** $\mathcal{T}$는 다음과 같이 정의된다. 집합 $U \subset X$가 열린 집합이라는 것은, 모든 점 $x \in U$에 대하여 $x \in B \subset U$를 만족하는 기저 원소 $B \in \mathcal{B}$가 존재하는 것과 동치이다.
> 
> 기저 $\mathcal{B}$에 의해 생성된 집합족 $\mathcal{T}$는 실제로 $X$ 위의 위상이 된다. 또한, $\mathcal{T}$는 $\mathcal{B}$의 원소들의 모든 합집합(union)들의 모임과 같다.
> 
> 기저는 위상을 정의하는 더 작고 간단한 "벽돌"들의 모임으로 생각할 수 있다. 모든 열린 집합을 명시하는 대신, 기저 원소들만 정의하면 그들의 합집합을 통해 전체 위상을 생성할 수 있으므로 위상을 더 효율적으로 다룰 수 있게 해준다. 예를 들어, 실수의 표준 위상은 모든 열린 구간들의 집합을 기저로 하여 생성된다.

> [!todo]- 위상 공간의 부분기저(subbasis)는 어떻게 정의되며, 부분기저로부터 위상을 어떻게 생성하는가?
> 
> 집합 $X$ 위의 위상에 대한 **부분기저(subbasis)** $\mathcal{S}$는 그 원소들의 합집합이 $X$ 전체를 덮는($\bigcup_{S \in \mathcal{S}} S = X$) 부분 집합들의 모임이다.
> 
> 부분기저 $\mathcal{S}$에 의해 **생성된 위상(generated topology)** $\mathcal{T}$는 $\mathcal{S}$의 원소들의 모든 유한 교집합(finite intersection)들의 모임을 기저($\mathcal{B}$)로 하여 생성된 위상이다. 즉, $\mathcal{T}$의 열린 집합들은 이 기저 원소들의 합집합으로 표현된다.
> 
> 부분기저 $\mathcal{S}$에 의해 생성된 집합족 $\mathcal{T}$는 $X$ 위의 위상이 된다. 이 위상 $\mathcal{T}$는 $\mathcal{S}$를 포함하는 가장 작은(coarsest) 위상이다.
> 
> 부분기저는 기저보다 더 기본적인 개념으로, 위상을 구성하는 최소한의 재료라고 할 수 있다. 예를 들어, 실수의 표준 위상($\mathbb{R}$)은 모든 열린 반직선(open rays)들의 모임인 $\{(-\infty, a) \mid a \in \mathbb{R}\} \cup \{(b, \infty) \mid b \in \mathbb{R}\}$을 부분기저로 하여 생성된다. 이 부분기저 원소들의 유한 교집합은 열린 구간 또는 공집합이 되어 표준 위상의 기저를 형성한다. 이처럼 복잡한 위상 구조를 매우 단순한 집합들로부터 체계적으로 구성할 수 있게 해준다는 점에서 부분기저의 의의를 찾을 수 있다.

> [!todo]- 두 위상을 비교할 때 기저를 어떻게 활용할 수 있는가?
> 
> 집합 $X$ 위에 두 위상 $\mathcal{T}$와 $\mathcal{T}'$이 있고, 각각에 대한 기저를 $\mathcal{B}$와 $\mathcal{B}'$라고 하자. 이때, 위상 $\mathcal{T}'$이 $\mathcal{T}$보다 더 세밀하다($\mathcal{T} \subset \mathcal{T}'$, finer)는 것과 다음 조건은 동치이다:
> * 모든 점 $x \in X$와 $x$를 포함하는 모든 기저 원소 $B \in \mathcal{B}$에 대하여, $x \in B' \subset B$를 만족하는 기저 원소 $B' \in \mathcal{B}'$가 존재한다.
> 
> 이 정리는 두 위상의 포함 관계를 모든 열린 집합을 비교할 필요 없이, 기저 원소들 사이의 포함 관계만으로 판별할 수 있게 해준다. 기저 $\mathcal{B}$의 각각의 "벽돌"($B$)이 기저 $\mathcal{B}'$의 더 작은 "벽돌"들($B'$)로 채워질 수 있다면, $\mathcal{B}'$이 생성하는 위상이 더 세밀하다고 직관적으로 이해할 수 있다.
> 
> **예시 (Example)**
> 실수 집합 $\mathbb{R}$ 위에서, 표준 위상($\mathcal{T}_{std}$)의 기저 $\mathcal{B}_{std}$는 모든 열린 구간 $(a, b)$들의 모임이다. 아래 극한 위상($\mathcal{T}_l$)의 기저 $\mathcal{B}_l$는 모든 반열린 구간 $[a, b)$들의 모임이다.
> 
> 임의의 열린 구간 $(a, b)$와 그 안의 점 $x$에 대하여, $x$를 포함하고 $(a, b)$에 포함되는 반열린 구간 $[x, b)$가 항상 존재한다. 따라서 아래 극한 위상 $\mathcal{T}_l$은 표준 위상 $\mathcal{T}_{std}$보다 더 세밀하다. 하지만 그 역은 성립하지 않는데, 기저 원소 $[c, d)$와 그 안의 점 $c$에 대해, $c$를 포함하고 $[c, d)$에 포함되는 열린 구간 $(a, b)$는 존재하지 않기 때문이다.

## 연속성(continuity)

> [!todo]- 위상 공간 사이의 함수가 연속(continuous)이라는 것은 어떻게 정의되는가?
> 
> 두 위상 공간 $(X, \mathcal{T}_X)$와 $(Y, \mathcal{T}_Y)$가 주어졌을 때, 함수 $f: X \to Y$가 **연속(continuous)** 이라는 것은, $Y$의 모든 열린 집합(open set) $V$에 대하여 그것의 역상(preimage) $f^{-1}(V)$가 $X$에서 열린 집합인 경우를 말한다.
> 
> $Y$의 위상에 대한 기저(basis) $\mathcal{B}$가 주어졌을 때, $f$가 연속 함수인 것은 모든 기저 원소 $B \in \mathcal{B}$에 대해 역상 $f^{-1}(B)$가 $X$에서 열린 집합인 것과 동치이다.
> 
> 이 정의는 미적분학에서 다루는 $\epsilon-\delta$ 정의를 일반화한 것이다. 열린 집합은 "가까움" 또는 "근방"의 개념을 위상적으로 표현하는데, 함수의 연속성은 $Y$에 있는 점 $f(x)$에 "가까운" 점들의 모임($V$)의 역상이, 원래 점 $x$에 "가까운" 점들의 모임($f^{-1}(V)$)이 됨을 의미한다. 즉, "가까운 점들을 가까운 점들로 보낸다"는 직관을 위상적으로 형식화한 것이다.

> [!todo]- 함수의 연속성을 판별하는 다른 동치 조건들에는 무엇이 있는가?
> 
> 두 위상 공간 $X, Y$와 함수 $f: X \to Y$에 대해, 다음 명제들은 모두 동치이다.
> 1. $f$는 연속이다.
> 2. $X$의 모든 부분집합 $A$에 대하여, $f(\overline{A}) \subset \overline{f(A)}$가 성립한다.
> 3. $Y$의 모든 닫힌 집합(closed set) $B$에 대하여, 역상 $f^{-1}(B)$는 $X$에서 닫힌 집합이다.
> 4. $X$의 각 점 $x$와 $f(x)$의 모든 근방(neighborhood) $V$에 대하여, $f(U) \subset V$를 만족하는 $x$의 근방 $U$가 존재한다.
> 
> 이 동치 조건들은 함수의 연속성을 다양한 관점에서 바라볼 수 있게 해준다.
> * **2. 폐포 조건** : 함수가 점들을 그들의 극한점(limit point)으로부터 멀리 떼어놓지 않음을 의미한다. $A$의 극한점은 $f$에 의해 $f(A)$의 극한점으로 보내진다는 것을 보여준다.
> * **3. 닫힌 집합 조건** : 열린 집합의 역상이 열린다는 정의와 쌍대(dual)적인 관계로, 위상 구조가 역상에 의해 잘 보존됨을 나타낸다.
> * **4. 근방 조건** : 미적분학의 $\epsilon-\delta$ 정의와 가장 유사한 형태로, "출력값($f(x)$)을 원하는 만큼 가깝게 만들기 위해, 입력값($x$)을 충분히 가깝게 잡을 수 있다"는 개념을 일반화한 것이다. 이를 통해 함수의 국소적(local)인 성질로서의 연속성을 이해할 수 있다.

> [!todo]- 위상동형사상(homeomorphism)이란 무엇이며, 연속성과 어떤 관계가 있는가?
> 
> 두 위상 공간 $X$와 $Y$ 사이의 함수 $f: X \to Y$가 다음 세 조건을 모두 만족할 때 **위상동형사상(homeomorphism)** 이라고 한다.
> 1. $f$는 전단사(bijective) 함수이다.
> 2. $f$는 연속(continuous) 함수이다.
> 3. 역함수 $f^{-1}: Y \to X$ 또한 연속 함수이다.
> 
> 만약 이러한 함수가 존재하면, 두 공간 $X$와 $Y$는 **위상동형(homeomorphic)** 이라고 한다.
> 
> **정리 (Theorem)**
> 전단사 함수 $f: X \to Y$가 위상동형사상인 것은, $f$가 열린 사상(open map)이면서 동시에 연속인 것과 동치이다.
> 
> 위상동형사상은 두 위상 공간이 위상적으로 "동일하다"는 것을 의미하는 가장 강력한 개념이다. 이는 단순히 점들의 집합 사이에 일대일 대응이 존재한다는 것을 넘어, 두 공간의 열린 집합 구조 사이에 완벽한 일대일 대응이 있음을 보장한다. 즉, 한 공간의 위상적 성질(예: 연결성, 컴팩트성)은 위상동형사상을 통해 다른 공간으로 그대로 전달된다. 연속성은 위상 구조를 보존하는 방향을 제시하지만, 위상동형사상은 양방향으로 구조가 완벽하게 보존됨을 의미한다.

> [!todo]- 기존의 연속 함수로부터 새로운 연속 함수를 만드는 기본적인 방법(정리)들을 설명하라.
> 
> 위상 공간 $X, Y, Z$가 주어졌을 때, 다음과 같은 규칙들이 성립한다.
> 1. (상수 함수, Constant function) : 함수 $f: X \to Y$가 $X$의 모든 원소를 $Y$의 한 점 $y_0$로 보낸다면, $f$는 연속이다.
> 2. (포함 사상, Inclusion map) : $A$가 $X$의 부분 공간일 때, 포함 사상 $j: A \to X$는 연속이다.
> 3. (합성 함수, Composite function) : 두 함수 $f: X \to Y$와 $g: Y \to Z$가 연속이면, 합성 함수 $g \circ f: X \to Z$도 연속이다.
> 4. (정의역의 제한, Restricting the domain) : 함수 $f: X \to Y$가 연속이고 $A$가 $X$의 부분 공간이면, 정의역을 $A$로 제한한 함수 $f|_A: A \to Y$도 연속이다.
> 5. (공역의 제한 또는 확장, Restricting or expanding the range) : 함수 $f: X \to Y$가 연속일 때, 만약 $Z$가 $f(X)$를 포함하는 $Y$의 부분 공간이면, 공역을 $Z$로 제한한 함수 $g: X \to Z$는 연속이다. 또한 $Y$가 $Z$의 부분 공간이면, 공역을 $Z$로 확장한 함수 $h: X \to Z$도 연속이다.
> 6. (붙임 보조정리, The Pasting Lemma) : $X = A \cup B$이고 $A, B$가 $X$의 닫힌 집합이라고 하자. 만약 두 연속 함수 $f: A \to Y$와 $g: B \to Y$가 모든 $x \in A \cap B$에 대해 $f(x) = g(x)$를 만족하면, 이 둘을 결합하여 정의한 함수 $h: X \to Y$는 연속이다. (이 정리는 $A, B$가 열린 집합일 때도 성립한다.)



부분공간(subspace)
곱공간(product space)
몫공간(quotient space)
거리공간(metric space)
완비성(completeness)
콤팩트성(compactness)
연결성(connectedness)
호모토피(homotopy)와 호모토피 동치(homotopy equivalence)
기본군(fundamental group)
덮개공간(covering space)
덱 변환(deck transformation)
사이퍼드-반 캄펜 정리(Seifert-Van Kampen theorem)
곡면의 분류(classification of surfaces)